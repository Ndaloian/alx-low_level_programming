#include "main.h"

/**
 * _abs - computes the absolute value of an integer
 * @int: input number as an integer
 * Return: Absolute value
 */
int _abs(int);
{
	if (int >= 0)
	{
		return (int);
	}
	else
	{
		return (int * -1);
	}
}
