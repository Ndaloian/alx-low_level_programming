#include "main.h"

/**
 * _abs - computes the absolute value of an integer
 * @int: input number as an integer
 * Return: Absolute value
 */
int _abs(int)
{
	int n = 0;

	if (n >= 0)
	{
		return (n);
	}
	else
	{
		return (n * -1);
	}
}
